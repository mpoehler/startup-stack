{
  "name": "startup-stack",
  "version": "1.0.0",
  "description": "A Firebase based Startup-Stack",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "tsc --project ./functions && rm -rf ./public/* && concurrently --kill-others 'firebase serve' 'tsc --project ./functions --watch' 'cd frontend && ./node_modules/.bin/vue-cli-service build --no-clean --dest ../public --watch' 'sleep 15 && browser-sync start --proxy \"localhost:5000\" --files \"public/*\"'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mpoehler/startup-stack.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/mpoehler/startup-stack/issues"
  },
  "homepage": "https://github.com/mpoehler/startup-stack#readme",
  "devDependencies": {
    "browser-sync": "^2.24.7",
    "concurrently": "^4.0.1",
    "typescript": "^3.0.3"
  }
}
